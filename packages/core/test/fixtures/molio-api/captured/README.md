# Captured Molio API responses

Written by `packages/app/scripts/capture-molio-fixtures.cjs`. Files are gitignored (this dir = developer scratch).

If you want to use a sample as a test fixture, **scrub identifiers** and move it to `../`.
